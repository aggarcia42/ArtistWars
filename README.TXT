This project started 06/04/2022.

/////NOT FINISHED YET ////

The main idea is to make a reddit /place but "improved".

Once the program is compiled you will have to execute the ./artwarserver file on the device you want the server to start.
Once you start the server you will be ask for an admin ID and a PW.
The size of the grid Horizontal Pixels * Vertical Pixels.

(Probably much data that i will be updating here)

Once you are done a config file will be saved in your ./servername/configuration

(More will be added (have to think about it first))